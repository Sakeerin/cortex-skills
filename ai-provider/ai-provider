#!/usr/bin/env bash
# ai-provider - shell wrapper for ai-provider skill
set -euo pipefail
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SKILL_DIR/scripts/ai_provider.py" "$@"
