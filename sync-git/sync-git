#!/usr/bin/env bash
# sync-git — shell wrapper for sync-git skill
# Add ~/.ai-skills/sync-git/ to PATH to use as: sync-git
set -euo pipefail
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SKILL_DIR/scripts/sync.sh" "$@"
