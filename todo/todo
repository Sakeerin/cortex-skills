#!/usr/bin/env bash
# todo — shell wrapper for todo skill
# Add ~/.ai-skills/todo/ to PATH to use as: todo list
set -euo pipefail
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SKILL_DIR/scripts/tasks.py" "$@"
