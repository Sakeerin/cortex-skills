#!/usr/bin/env bash
# token-budget - shell wrapper for token-budget skill
set -euo pipefail
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SKILL_DIR/scripts/token_budget.py" "$@"
