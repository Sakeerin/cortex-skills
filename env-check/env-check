#!/usr/bin/env bash
# env-check — shell wrapper for env-check skill
# Add ~/.ai-skills/env-check/ to PATH to use as: env-check
set -euo pipefail
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SKILL_DIR/scripts/env_check.py" "$@"
